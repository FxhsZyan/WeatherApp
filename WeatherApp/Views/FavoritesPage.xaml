<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:WeatherApp.Models"
             x:Class="WeatherApp.Views.FavoritesPage"
             Title="Favorite Cities">

    <Grid RowDefinitions="*, Auto">

        <Image Source="weather_bg_day.png" Aspect="AspectFill" Grid.RowSpan="2" />

        <ScrollView Grid.Row="0" Padding="20">
            <VerticalStackLayout Spacing="15">

                <Label Text="Favorite Cities"
                       FontSize="28"
                       FontAttributes="Bold"
                       TextColor="White"
                       HorizontalOptions="Center"
                       Margin="0,20,0,10"/>

                <CollectionView x:Name="FavoritesCollection"
                                ItemsSource="{Binding Favorites}">
                    <CollectionView.EmptyView>
                        <Label Text="No favorites yet. Search for a city and save it!"
                               FontSize="16"
                               TextColor="White"
                               HorizontalOptions="Center"
                               HorizontalTextAlignment="Center"
                               Margin="20"/>
                    </CollectionView.EmptyView>
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="models:FavoriteCity">
                            <Frame Margin="0,5"
                                   Padding="15"
                                   BackgroundColor="#80FFFFFF"
                                   CornerRadius="15"
                                   HasShadow="False">
                                <Grid ColumnDefinitions="*, Auto, Auto">

                                    <VerticalStackLayout Grid.Column="0"
                                                         VerticalOptions="Center">
                                        <Label Text="{Binding Label}"
                                               FontSize="18"
                                               FontAttributes="Bold"
                                               TextColor="#1a1a2e"/>
                                        <Label Text="{Binding CityName}"
                                               FontSize="13"
                                               TextColor="#555"/>
                                        <Label Text="{Binding DateAdded, StringFormat='Added: {0:MMM d, yyyy}'}"
                                               FontSize="11"
                                               TextColor="#888"/>
                                    </VerticalStackLayout>

                                    <Button Grid.Column="1"
                                            Text="✏️"
                                            FontSize="18"
                                            BackgroundColor="Transparent"
                                            Clicked="OnEditClicked"
                                            VerticalOptions="Center"/>

                                    <Button Grid.Column="2"
                                            Text="🗑️"
                                            FontSize="18"
                                            BackgroundColor="Transparent"
                                            Clicked="OnDeleteClicked"
                                            VerticalOptions="Center"/>

                                </Grid>

                                <Frame.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="OnCityTapped"/>
                                </Frame.GestureRecognizers>

                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

            </VerticalStackLayout>
        </ScrollView>

    </Grid>
</ContentPage>